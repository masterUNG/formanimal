import 'package:flutter/material.dart';

class DisplayHeatDetaction extends StatelessWidget {
  const DisplayHeatDetaction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}